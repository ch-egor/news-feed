{% extends 'base.html.twig' %}

{% block title %}All News{% endblock %}

{% block body %}
    {% for newsItem in newsItems %}
        <section class="news-item">
            <h1>
                <a href="{{ path('viewNewsItem', { 'urlName': newsItem.urlName }) }}">
                    {{ newsItem.title }}
                </a>
            </h1>
            <p>{{ newsItem.preview }}</p>
        </section>
    {% endfor %}
{% endblock %}
